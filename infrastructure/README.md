# Infrastructure

This folder contains non-code infrastructure artifacts: deployment configs, IaC scripts, and operational runbooks.

## Structure (planned)

```
infrastructure/
├── github/        → GitHub Actions reusable workflows, repo settings
└── deploy/        → Deployment notes for Vercel (frontend) and Render (backend)
```

Populated during Phase 1.5 (CI/CD scaffold) and beyond.
